spring:
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: ${CLIENT_ID}
            client-secret: ${CLIENT_SECRET}
            scope: read:user
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html

app:
  roles:
    organisatoren:
      - GazmendK
    korrektoren:
    studenten:
